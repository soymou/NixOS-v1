# Add your reusable modules to this directory
# These should be stuff you would like to share with others, not your personal configurations.
{
    sddm = import ./ui/sddm;
    minecraft-servers = import ./minecraft;
}
